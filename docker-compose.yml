services:
  movies-api:
    container_name: movies-api
    build:
      # Build the service from the local Dockerfile in the repository root.
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${PORT:-8080}:8080"
    # Restart automatically unless the container is stopped manually.
    restart: unless-stopped
